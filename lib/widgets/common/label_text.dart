import 'package:flutter/material.dart';
import 'package:quomia/utils/design_system.dart';

/*class LabelText extends StatelessWidget {
  final String data;
  final bool isLabel;

  const LabelText({required this.data, required this.isLabel, super.key});

  @override
  Widget build(BuildContext context) {
    TextStyle style = _computeTextStyle();

    return const Text(
      'data',
      style: style,
    );
  }

  TextStyle _computeTextStyle() {
    if (isLabel) {
      return _normalText();
    }
    return _normalText();
  }

  TextStyle _normalText() {
    return TextStyle(
      fontFamily: DesignSystem.fonts.fontFamily,
    );
  }
}*/
